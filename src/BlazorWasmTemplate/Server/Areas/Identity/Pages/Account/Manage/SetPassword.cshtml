@page
@model SetPasswordModel
@{
    ViewData["Title"] = "Set password";
    ViewData["ActivePage"] = ManageNavPages.ChangePassword;
}

<h3 class="header">Set your password</h3>
<partial name="_StatusMessage" for="StatusMessage" />
<p class="text-info">
    You do not have a local username/password for this site. Add a local
    account so you can log in without an external login.
</p>
<div class="row">
    <div class="col m12">
        <form id="set-password-form" method="post">
            <div asp-validation-summary="ModelOnly" class="helper-text"></div>
            <div class="input-field col s12">
                <input asp-for="Input.NewPassword" class="validate" autocomplete="new-password" />
                <label asp-for="Input.NewPassword"></label>
                <span asp-validation-for="Input.NewPassword" class="helper-text"></span>
            </div>
            <div class="input-field col s12">
                <input asp-for="Input.ConfirmPassword" class="validate" autocomplete="new-password" />
                <label asp-for="Input.ConfirmPassword"></label>
                <span asp-validation-for="Input.ConfirmPassword" class="helper-text"></span>
            </div>
            <button type="submit" class="btn waves-effect waves-light">Set password</button>
        </form>
    </div>
</div>

@section Scripts {
    <partial name="_ValidationScriptsPartial" />
}
